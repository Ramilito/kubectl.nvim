[package]
name = "kubectl_client"
version = "0.1.0"
edition = "2021"

[lib]
name = "kubectl_client"
path = "lua/kubectl/client/rust/lib.rs"
crate-type = ["cdylib"]

[dependencies]
mlua = { version = "0.10.2", features = ["module", "luajit"] }

[build]
rustflags = ["-Clink-arg=-Wl,-soname,custom_name.so"]
[build-dependencies]
